import { PostFilter3Pipe } from './post-filter-3.pipe';

describe('PostFilter3Pipe', () => {
  it('create an instance', () => {
    const pipe = new PostFilter3Pipe();
    expect(pipe).toBeTruthy();
  });
});
