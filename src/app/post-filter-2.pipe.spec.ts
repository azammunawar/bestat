import { PostFilter2Pipe } from './post-filter-2.pipe';

describe('PostFilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new PostFilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
